# 前端问题

## 全局

## 首页
- [已修复] 首页顶部标签，全部，实时，离线，项目空间切换没有效果，都一样：按模块类型重新分流首页统计、任务预览和项目空间列表。

## 任务列表
- [已修复] 顶部项目空间名后面有 ·离线或·实时，因为后面已经有离线还是实时标记了，这个就重复展示了：顶部项目空间下拉只保留项目空间名称，模块类型只保留右侧徽标。
- [已修复] 框的长度比较长：收窄了顶部项目空间切换器和任务概览卡片的网格宽度。
- [已修复] 旁边的梅花标记，上面有12数字，有什么作用？：移除了无实际业务意义的通知入口和角标。
- [已修复] 任务列表框中，当前列表来自最近一次git扫描稿，有两行展示，这个可以变成刷新后，自动消失，或弹窗提示，不需要在前端一直显示：最近草稿提示改为不再常驻展示，刷新结果只保留轻量反馈。
  - 刷新会弹窗显示，隔一会自己消失，不要一直展示
- [已修复] git变更摘要，也不需要，可以和上面的合并成一个区域：已移除单独摘要卡片，信息并入顶部 Git 概览区域。


## 配置管理
- [已修复] 元数据同步这里，每个小块，太占空间了，看是否可以合并成一行，或缩小宽度，太丑了：压缩为更紧凑的一行式信息区，并减小卡片块尺寸。
  - [已修复] 顶部按钮与图区对齐：调整标题区和按钮组换行策略，避免按钮堆叠和图标错位。
- [已修复] 项目空间git绑定，未绑定仓库地址就留空：未绑定仓库地址时保持空输入框，不再补默认值。
  - [已修复] 空仓库地址不再显示占位默认仓库：保留空值，避免误导。
- [已修复] 项目空间git绑定，数据源，项目空间，操作按钮有些应该要统一，比如编辑，保存，启用，停用等：映射类表格已统一为编辑态再保存，按钮文案和显示时机保持一致。
- [已修复] 项目空间映射，环境名也是重复信息展示，有了模块类型，就不要有括号里的重复展示了：项目空间下拉候选改为只显示空间名称，模块信息单独放在模块类型列。
  - [已修复] 项目空间映射下拉候选已去掉括号里的模块冗余信息：模块只保留在独立列展示。
- [已修复] 所有模块，只有点编辑，才可以有编辑更新；否则不能进行更新操作：项目空间映射和数据源映射已切换为默认只读，进入编辑态后才允许修改。
  - [已修复] 项目空间 Git 绑定必须先点编辑后才能修改并保存：取消直接可写状态。
  - [已修复] Git 绑定表头已补排序标识：支持排序反馈。
- [已修复] 项目空间映射，下拉框操作，和数据源映射的形式不一样，会在原值右边新出现一个下拉框，很奇怪：项目空间映射新增行和编辑态已统一为原位切换，不再在右侧额外长出下拉框。
  - [已修复] 编辑态下拉框改为原位切换：同时修复保存或取消后跳回页面顶部的问题。

## git代码管理
- [已修复] 项目空间的值不是真实值：Git 管理页改为读取平台库中的真实项目空间 Git 绑定数据，不再写死演示值。

---
# 后端问题
